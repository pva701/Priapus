fun int gcd(int x, int y) {
    int t;
    while (y != 0) {
        x = x % y;
        t = y;
        y = x;
        x = t;
    }
    return x;
}

start {
    int x = 15, y = 12;
    int res = gcd(x, y);
}
