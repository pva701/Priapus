fun int gcd(int x, int y) {
    int t;
    while (y != 0) atomic {
        x = x % y;
        t = y;
        y = x;
        x = t;
    }
    return x;
}

start {
    int x = 18, y = 12;
    int res = gcd(x, y);
    return res;
}
