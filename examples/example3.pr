fun int sum(int x, int y) {
    return x + y;
}

int result = 0;

start {
  int i = 0, j = 0;
  int n = 10, m = 20;
 
  while (i < n) {
    while (j <= i) {
      result = result + sum(i, j);
      j = j + 1;
    }
    i = i + 1;
  }

  return result;
}
